{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
			"command": "make -j16",
				
        }
    ],
    "version": "2.0.0"
}

/*{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}*/